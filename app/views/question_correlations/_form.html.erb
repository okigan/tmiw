<%= form_for(@question_correlation) do |f| %>
  <% if @question_correlation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_correlation.errors.count, "error") %> prohibited this question_correlation from being saved:</h2>

      <ul>
      <% @question_correlation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br />
    <%= select("question_correlation", "question_id", Question.all.collect { |q| [q.id.to_s + " " + q.body, q.id] } ) %>
  </div>
  <div class="field">
    <%= f.label :followup_question_id %><br />
    <%= select("question_correlation", "followup_question_id", Question.all.collect { |q| [q.id.to_s + " " + q.body, q.id] } ) %>
  </div>
  
  <div class="field">
    <%= f.label :points %><br />
    <%= f.text_field :points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
