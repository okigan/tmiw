<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
   <th></th>
   <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
    <td><%= link_to 'Questions Asked', user_questions_path(user) %></td>
    <td><%= link_to 'Answers Given', user_answers_path(user) %></td>
</tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
